# Which backend the app uses: "local" (single device, default) or "supabase".
VITE_BACKEND=local

# Supabase project (only needed when VITE_BACKEND=supabase).
# The frontend uses ONLY the publishable key. Never put a service_role key here.
VITE_SUPABASE_URL=https://tzqiorwfkjqpqjefklrf.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_1uessLo_WNKsLewylxfkEw_7I_t1z46

# Optional: base path for the static build (GitHub Pages). Defaults to /buraco/.
# VITE_BASE=/buraco/
